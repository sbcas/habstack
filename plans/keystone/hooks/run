#!/bin/sh

echo "Starting keystone (admin) ..."

{{pkg.svc_path}}/uwsgi {{pkg.path}}/keystone-wsgi-admin

echo "Starting keystone (public) ..."

{{pkg.svc_path}}/uwsgi {{pkg.path}}/keystone-wsgi-public
